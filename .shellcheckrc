# @url https://raw.githubusercontent.com/github/super-linter/main/TEMPLATES/.shellcheckrc

# @url https://github.com/github/super-linter/issues/1633
# `.shellcheckrc` is not picked up by `super-linter` if placed in
# `.github/linters/`. Placing `.shellcheckrc` into the repository root seems to
# work. 

# ShellCheck can't follow non-constant source. Use a directive to specify
# location.
disable=SC1090

# ShellCheck: SC1091 – Not following: (error message here).
disable=SC1091
